export const UserOutput = (props) => {
    // export const UserOutput = ({mainText}) => {
    const { mainText } = props
    return (
        <div>
            {mainText}
        </div>
    )
}
